import pandas as pd

# Define the sheets as dataframes
global_attributes = pd.DataFrame({
    "Attribute": [
        "Conventions", "title", "institution", "source", "history", "project_id",
        "activity_id", "experiment_id", "parent_experiment_id", "grid_label",
        "nominal_resolution", "geospatial_lat_min", "license"
    ],


